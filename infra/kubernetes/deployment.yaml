apiVersion: apps/v1
kind: Deployment
metadata:
  name: gedtotal-custody-service
  namespace: gedtotalap-apps
spec:
  replicas: 1
  selector:
    matchLabels:
      app: gedtotal-custody-service
  template:
    metadata:
      labels:
        app: gedtotal-custody-service
    spec:
      containers:
        - name: gedtotal-custody-service
          image: docker.io/eurival/gedtotal-custody-service
          imagePullPolicy: Always
          ports:
            - containerPort: 8092
          envFrom:
            - configMapRef:
                name: gedtotal-custody-service-config
            - secretRef:
                name: gedtotal-custody-service-secrets
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8092
            initialDelaySeconds: 20
            periodSeconds: 15
            timeoutSeconds: 5
            failureThreshold: 6
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8092
            initialDelaySeconds: 60
            periodSeconds: 20
            timeoutSeconds: 5
            failureThreshold: 5
          resources:
            requests:
              cpu: "200m"
              memory: "512Mi"
            limits:
              cpu: "1000m"
              memory: "1Gi"
